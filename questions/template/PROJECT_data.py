"""PROJECT data module — all constants, fetching, and computation.

Fill in fetch_* and compute_* functions here. Cache all API responses under
cache/ so repeated runs don't hit the network. This is the only module that
calls external APIs; PROJECT_plots.py and PROJECT_analysis.py import from here.
"""
