# TODO: Title — Methods

```{=typst}
#align(center)[_Draft_]
```

::: {.content-visible when-format="html"}
<p style="text-align:center"><em>Draft</em></p>
:::

<!-- Follow ../STATS_EXPLAINER_GUIDE.md (shared best practices) plus the "knows statistics, but rusty" row in ../CLAUDE.md.
  CARDINAL RULE: every method named here must match what PROJECT_analysis.py / nbakit actually runs — verify against the code and PROJECT_results.md before writing. Describing an estimator the code doesn't use is the worst failure this doc can make.
  Use the REAL terms (binomial GLM, cluster-robust SE, ridge/MAP, QLR, bootstrap, confidence interval) and refresh each on first use; don't dumb down (that's findings tier), don't pile on unreconstructable jargon.
  Every cited number is an f("section.metric") template call, never hand-typed. -->

<!-- ORIENTATION NOTE (Requirement 1): keep it short. State the audience, that this is the METHODS companion (findings = plain narrative; investigation = evidence with p-values; this = HOW the numbers were computed), and the section order. -->
This is the methods companion to the findings report: it explains how each number in `PROJECT_results.md` was computed, for a reader who knows statistics but is rusty.
Every method named here matches what `PROJECT_analysis.py` (and `nbakit/`) actually runs.
TODO: state the section order (results order for Layout A; technique grouping for Layout B).

<!-- LAYOUT (Guide: "Two layouts"): pick ONE and delete the other.
  A — pipeline walkthrough: one `## N. Title (`run_xxx`)` per analysis, in PROJECT_results.md order. Best for analysis-driven projects (see home_court, knicks).
  B — topical methods catalog: group by technique family. Best for methods-survey projects (see player_rating_overview). -->

---

## 0. The Dataset

<!-- Requirement 2: how the analysis frame(s) are built — source, date range, one-row-per-WHAT, key columns and WHY each exists. Name the endpoints/tables PROJECT_data.py actually pulls. If many analyses share one frame, describe it once here and have later sections point back. -->

TODO: describe the analysis dataset(s): source, range, granularity, key columns and why.

## 1. TODO first analysis / method (`run_xxx`)

<!-- Per-item template (Guide: "The per-item template") — the same four beats for every item, Layout A or B: -->
<!-- The data — which inputs/subset this uses (or "the frame from section 0"). -->
<!-- The method — the REAL name + what it estimates; if it recurs, one clause + a pointer to the Methods Toolbox rather than a full re-explanation. -->
<!-- Why this method — why it fits the question / the alternative it beats. -->
<!-- What the result means — how to read the output (coef sign/size, CI, p, test stat) with the headline number via an f("...") call. Add a "Why this chart" beat when the section owns a figure. -->

TODO: first analysis, following the four-beat template.

<!-- ...one section per analysis (Layout A) or per technique family (Layout B)... -->

## Recurring Methods: Quick Reference

<!-- Requirement 5 (DRY core): define every method used in 2+ places ONCE here; the sections above reference it instead of re-explaining. Entry shape:
  **Name (real term):** one-line refresh of what it is · how to read its output · the key assumption / when it misleads.
  A method used in exactly one place can be defined inline in that section instead. -->

TODO: one compact entry per recurring method.

## Limitations

<!-- Requirement 7: what the methods can and cannot establish — correlation vs causation, what the design identifies, sample-size caveats. Naming the limit is part of the job. -->

TODO: what the methods can and cannot establish.
<# Template: cite registered facts with f("section.metric") once PROJECT_analysis.py registers them. See docs/PROJECT_facts.json. -#>
