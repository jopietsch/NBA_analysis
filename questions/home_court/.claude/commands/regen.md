Regenerate all PDFs and HTML files for the home_court project in the correct cascade order.

Run from the home_court project directory:

```bash
bash regen_all.sh
```

Report which files were saved and flag any errors.
